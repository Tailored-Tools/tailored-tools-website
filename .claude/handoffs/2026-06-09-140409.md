---
type: wrap-up
project: /Users/james/Projects/Tailored-Tools/tailored-tools-website
project_name: Tailored Tools Website
timestamp: 2026-06-09T13:50:00Z
trigger: manual
---

## What We Did

- **Self-hosted AO form replacing Tally** — end-to-end pipeline:
  - Cloudflare Worker at `worker/ao-form/` (validates, honeypot + time-check anti-spam, calls Resend)
  - Deployed to `https://ao-form.tailored-tools.workers.dev/ao-score` on the Hello@tailored-tools.com CF account
  - Resend sender flipped to verified `noreply@send.tailored-tools.com`
  - Native form HTML + `/js/ao-form.js` posting via fetch
  - All Tally infrastructure removed: `js/consent.js`, consent banner CSS, `.tally-embed` blocks, privacy § 6.3
- **Logo migration**: user-supplied transparent PNG (`tt-logo-light.png`) replacing `.svg` site-wide; CSS switched to `background-size: contain`
- **Dedicated project pages**: `/projects/sonic-sanctuary` and `/projects/amanda-jane-wellness` created; `/projects` restructured as a 4-card landing
- **About page community section** linked to `/community/`
- **Stat labels & copy on SS project page** tightened; "dogfooded" replaced with "in daily use by the person who built it"; "family-run" → "national" on PI card
- **Custom Platforms icon** swapped from cryptic two-squares to Lucide `boxes` (3D stacked cubes)
- **Homepage hero modernised**:
  - Aurora mesh gradient blobs + film-grain noise overlay
  - Replaced constellation with three glass platform mockups (Website / App / OS) with brand-orange tags
  - Cards gently bob (no swapping; user rejected slide/cycle animations as "ghostly" and "weird")
- **Section CTA text-colour bug** on `/projects/*` fixed via specificity bump (`.section.section-cta`)

## Decisions Made

- **Path A: self-hosted form** chosen over Tally embed — owns the data, drops the consent banner entirely, no third-party JS. Trade-off: 1 CF Worker + Resend domain verification work, paid off.
- **`workers.dev` subdomain renamed** from default `winter-snowflake-f68f` → `tailored-tools` (brand-aligned, visible in DevTools).
- **No AI chatbot/Clippy pet for now** — user proposed at end of session but acknowledged "maybe being silly". Brand tension (underwriter-ready professional vs cartoon pet) + multi-session scope. Deferred for a fresh `/brainstorm` or `/gsd:new-milestone` session.
- **Hero card animation kept simple**: gentle Y-axis bob only. Slide-in/out animation rejected by user; depth-fade rejected as "ghostly".
- **AO funnel emails** point to `ao@tailored-tools.com`; rest of site uses `hello@`.

## Files Changed

- `worker/ao-form/{wrangler.toml, src/index.js, README.md}` (new — CF Worker)
- `js/ao-form.js` (new — form submit handler)
- `js/consent.js` (deleted)
- `index.html` (hero rebuild: aurora, grain, glass cards with Website/App/OS tags, gentle bob)
- `projects.html` (restructured to landing-card grid)
- `projects/sonic-sanctuary.html`, `projects/amanda-jane-wellness.html` (new)
- `about.html` (community link, project deep-links)
- `styles.css` (consent banner CSS removed; logo bg-size: contain)
- `privacy.html` (Tally removed; § 6.3 removed; Resend processor entry updated)
- `tt-logo-light.png` (new, RGBA transparent — replaced .svg refs site-wide)
- `.gitignore` (added — `worker/ao-form/node_modules/`)
- Various copy fixes across blog/project pages

## Current State

- **Site live and committed**, latest at `78f58f9` on `main`. GitHub Pages auto-deploys; CF in front.
- **AO form working end-to-end** — verified test submission landed in `ao@`.
- **Worker deployed** on Hello@tailored-tools.com CF account; `RESEND_API_KEY` secret set.
- **Cache buster at `styles.css?v=14`** — bump if more CSS changes.
- **`.DS_Store` and `.superpowers/brainstorm/` files** accidentally committed earlier in session (commit `db1a3ae`). Harmless but should be added to `.gitignore` and removed from git index in a follow-up.

## Blockers

- None blocking. Open follow-ups (not blockers):
  - **ICO ZA registration number** still waiting from ICO (separate email to CSN4737051 contact security number). When it lands, add to `privacy.html` § 1 + footer disclosure.
  - **Cyber Essentials** SAQ not yet started — user can do solo via IASME (~£320, micro-business tier).
  - **Hiscox PI policy** still being underwritten.
  - **GitHub Pages native SSL** on CF Flexible workaround pending GH Support ticket clearance for `bad_authz` ACME state.
  - **AJW + Onizion testimonials** pending; draft AJW one is in conversation history if Amanda approves.
  - **SS Stripe total + events run count + PI interpreter requests current count** — user has yet to gather.

## Next Steps

- [ ] **Clean up accidentally-committed `.DS_Store` and `.superpowers/brainstorm/`** files — add to `.gitignore`, `git rm --cached`, commit. Small follow-up.
- [ ] **Decide AI assistant question properly** — fresh session with `/brainstorm` if user wants to revisit the Clippy-style pet idea. Discuss persona (professional vs playful), scope (chat widget vs RAG vs ask-the-site), brand fit. Don't start building until that's decided.
- [ ] **Wire Tally form notifications to `ao@`** — Tally is dead now but if any old Tally inbox routing was set up at `tally.so` admin, can be cleaned up there too (low priority).
- [ ] **When ICO ZA arrives**: single Edit to `privacy.html` § 1 and footer of all pages with the ZA######## number.
- [ ] **When Cyber Essentials cert issues**: add to footer + ethics page + add a recognition strip if collection grows.
